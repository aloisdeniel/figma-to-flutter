import 'package:flutter/widgets.dart';

class ResizablePanels extends StatelessWidget {
  const ResizablePanels({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
